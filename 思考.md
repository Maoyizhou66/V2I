
整体思路：决策级协作思路

时空同步：
1、采用轨迹形状匹配 + 重心对齐的抗差法 --- 得到路端-世界的转换矩阵。

2、采用互相关时延估计 --- 解决时钟同步问题(杀手锏)
利用速度大小做互相关。不需要坐标系转换

3、先做时间互相关（基于速度不变量），再做空间SVD，时间是准的，空间标定时用的点对也是同一时刻的，SVD算出来的矩阵精度会提升一个档次。

原始轨迹 → 计算速度序列 → 互相关求时延τ → 将路端轨迹整体平移τ 秒 → 此时两条轨迹在时间上“锁死” → 送入空间对齐模块。

