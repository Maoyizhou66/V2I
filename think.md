
构建车-路感知误差栅格地图
“查表 + 马氏距离”

获取每个路端转世界的转换矩阵 --- 动态标定探针
1、数据采集；2、时间戳硬同步；3、SVD解刚体变换

离线构建栅格地图的方式:
1、让UWB的采集车在直路/低环/高环跑。
2、同时记录每种场景的数据。
    车端感知输出自车坐标系的目标P_detect_vehicle (停放在旁边车道的目标车)
    路端感知输出的同一目标的坐标P_detect_road()
    直接计算“车端输出坐标”和“路端输出坐标”的差值作为残差
