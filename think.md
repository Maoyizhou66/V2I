
语义场景网格构建：
1、开着采集车，在直道、低环、高环分别跑几圈，记录车端/路端/真值坐标。
2、写一个离线脚本，计算每个场景下的均值偏差和协方差，生成上述 scene_trust_map 字典。
3、将在线代码中的“交叉注意力+编码器”整块替换为“查表 + 马氏距离”。

获取每个路端转世界的转换矩阵 --- 动态标定探针
1、数据采集；2、时间戳硬同步；3、SVD解刚体变换

构建语义场景地图的方式

